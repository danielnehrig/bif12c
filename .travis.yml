language: c
os:
  - linux
sudo: false
script:
  - "gcc -fprofile-arcs -c -Wall *.c"
  - "gcov calc.gcno"
after_success:
  - bash <(curl -s https://codecov.io/bash)
addons:
  apt:
    packages:
      - curl
      - wget
      - gcc
      - curl
