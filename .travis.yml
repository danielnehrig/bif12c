language: c
os:
  - linux
sudo: false
script:
  - "gcc -coverage calc.c -o calc"
  - "./calc < calc_test.txt"
  - "gcov calc.c"
  - "gcc -std=c99 -coverage auswahl.c -o auswahl"
after_success:
  - bash <(curl -s https://codecov.io/bash)
